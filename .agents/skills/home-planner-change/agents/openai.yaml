interface:
  display_name: 'Change Home Planner'
  short_description: 'Implement and debug the planner'
  default_prompt: 'Use $home-planner-change to implement the requested Home Planner change and verify it.'
policy:
  allow_implicit_invocation: true

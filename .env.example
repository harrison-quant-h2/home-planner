# Optional direct MCP lookup. Copy to .env.local (ignored by Git).
# Obtain the URL, bearer credential, and read-only tool name from YOUR provider.
# Zillow's ChatGPT app does not supply a documented public endpoint here.
ZILLOW_MCP_URL=
ZILLOW_MCP_TOOL=
ZILLOW_MCP_QUERY_FIELD=query
ZILLOW_MCP_TOKEN=
